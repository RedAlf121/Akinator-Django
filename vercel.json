{
    "builds": [{
      "src": "Akinator/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.12.3" },
      "buildCommand": "pip install -r requirements.txt && python manage.py tailwind install && python manage.py collectstatic --noinput && python manage.py migrate"
    }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "Akinator/wsgi.py"
      }
    ]
}